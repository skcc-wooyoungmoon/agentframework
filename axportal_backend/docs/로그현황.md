# AX Portal Backend 로깅 분석 보고서

## 📋 개요

이 문서는 AX Portal Backend 프로젝트의 로깅 시스템에 대한 종합적인 분석 보고서입니다. 
Spring Boot 기반 애플리케이션의 환경별 로깅 설정, 패턴, 파일 위치 및 구성을 상세히 분석합니다.

**분석 일시**: 2025년 11월 14일  
**분석 대상**: 5개 환경 (elocal, edev, local, dev, prod)  
**로깅 프레임워크**: Logback (Spring Boot 기본)

---

## 🏗️ 로깅 아키텍처 구조

### 로깅 시스템 구성도

```
┌─────────────────────────────────────────────────────────────┐
│                    Spring Boot Application                   │
├─────────────────────────────────────────────────────────────┤
│  Application Layer Logging                                  │
│  ├─ Controller Logging    (HTTP 요청/응답, API 호출)        │
│  ├─ Service Logging       (비즈니스 로직, 트랜잭션)          │
│  ├─ Repository Logging    (데이터베이스 액세스)             │
│  ├─ Batch Logging         (배치 작업, 스케줄링)             │
│  └─ External API Logging  (외부 시스템 연동)                │
├─────────────────────────────────────────────────────────────┤
│  Framework Layer Logging                                    │
│  ├─ Spring Security       (인증/인가, 보안)                 │
│  ├─ Hibernate/JPA         (SQL, 트랜잭션)                   │
│  ├─ HikariCP              (커넥션 풀)                        │
│  └─ Feign Client          (외부 API 호출)                   │
├─────────────────────────────────────────────────────────────┤
│              Logback Configuration (logback-spring.xml)     │
│  ├─ Profile-based Configuration (환경별 설정)               │
│  ├─ Appender Configuration (출력 대상 설정)                 │
│  ├─ Pattern Configuration  (로그 형식 설정)                 │
│  └─ Logger Configuration   (로거별 레벨 설정)               │
├─────────────────────────────────────────────────────────────┤
│                      Output Destinations                    │
│  ├─ Console Output        (개발환경: elocal, local)         │
│  ├─ File Output           (개발/운영: edev, dev, prod)      │
│  ├─ JSON Format           (구조화된 로그: dev, prod)        │
│  └─ Separate Error Files  (에러 전용 파일: dev, prod)      │
└─────────────────────────────────────────────────────────────┘
```

### 환경별 로깅 전략

| 환경 | 출력 대상 | 로그 형식 | 주요 목적 | 데이터베이스 |
|------|-----------|-----------|-----------|-------------|
| **elocal** | Console | Custom Pattern | 외부 로컬 개발자 친화적 콘솔 출력 | PostgreSQL |
| **local** | Console | Custom Pattern | 로컬 개발 환경 | Tibero |
| **edev** | Console + File | JSON + Custom | 외부 개발 환경 테스트 | PostgreSQL |
| **dev** | File Only | JSON | 개발 환경 로그 수집 | Tibero |
| **prod** | File Only | JSON | 운영 환경 모니터링 | Tibero |

---

## 📁 로그 파일 위치 및 명명 규칙

### 기본 파일 위치 설정

```yaml
# 공통 설정 (logback-spring.xml)
LOG_PATH: ${LOG_PATH:-logs}              # 기본: logs 디렉토리
LOG_FILE: ${LOG_FILE:-axportal-backend}  # 기본: axportal-backend
```

### 환경별 로그 파일 구조

#### 1. **elocal 환경** (PostgreSQL 외부 로컬)
```
출력 대상: 콘솔 (파일 생성 없음)
용도: 외부 로컬 개발자 실시간 디버깅
데이터베이스: PostgreSQL
```

#### 2. **local 환경** (Tibero 로컬)
```
출력 대상: 콘솔 (파일 생성 없음)
용도: 로컬 개발자 실시간 디버깅
데이터베이스: Tibero
```

#### 3. **edev 환경** (PostgreSQL 외부 개발)
```
logs/
├── axportal-backend.log                    # 일반 로그 (JSON 형식)
├── axportal-backend-2025-01-23-0.log      # 롤링 로그 파일
├── axportal-backend-2025-01-23-1.log      # 롤링 로그 파일
└── ...
```

#### 3. **dev 환경** (Tibero 개발)
```
logs/
├── axportal-backend.json                  # 일반 로그 (JSON 형식)
├── axportal-backend-error.json            # 에러 전용 로그
├── axportal-backend-2025-01-23-0.json     # 일반 로그 롤링
├── axportal-backend-error-2025-01-23-0.json # 에러 로그 롤링
└── ...
```

#### 4. **prod 환경** (운영)
```
logs/
├── axportal-backend.json                      # 일반 로그
├── axportal-backend-error.json                # 에러 로그
├── axportal-backend-performance.json          # 성능 로그
├── axportal-backend-audit.json                # 감사 로그 (보안)
├── axportal-backend-2025-01-23-0.json         # 일반 로그 롤링
├── axportal-backend-error-2025-01-23-0.json   # 에러 로그 롤링
├── axportal-backend-performance-2025-01-23-0.json # 성능 로그 롤링
├── axportal-backend-audit-2025-01-23-0.json   # 감사 로그 롤링
└── ...
```

### 로그 파일 롤링 정책

| 환경 | 파일 크기 | 보관 기간 | 총 용량 제한 |
|------|-----------|-----------|-------------|
| **edev** | 100MB | 14일 | 2GB |
| **dev** | 100MB | 30일 | 3GB |
| **prod** | 100MB | 30일 (에러: 60일, 감사: 90일) | 3GB (에러: 2GB, 감사: 5GB) |

---

## 🎨 로그 포맷 및 패턴

### 1. **콘솔 로그 패턴** (elocal, local, edev 콘솔)

```
패턴: %d{yyyy-MM-dd HH:mm:ss.SSS} %5p %-40.40logger{39}: %m%n%ex
```

**예시 출력:**
```
2025-01-23 14:30:25.123  INFO c.s.a.controller.AuthController    : 사용자 로그인 요청 - username: testuser01
2025-01-23 14:30:25.125  WARN c.s.a.service.UserService         : 사용자 권한 확인 - userId: 12345, role: USER
2025-01-23 14:30:25.127 ERROR c.s.a.client.SktaiAuthService     : SKTAI 인증 실패 - username: testuser01, error: Connection timeout
```

**패턴 구성 요소:**
- `%d{yyyy-MM-dd HH:mm:ss.SSS}`: 타임스탬프 (밀리초 포함)
- `%5p`: 로그 레벨 (5자리 고정폭)
- `%-40.40logger{39}`: 로거명 (40자리 고정폭, 좌측 정렬)
- `%m`: 로그 메시지
- `%n%ex`: 줄바꿈 + 예외 스택트레이스

### 2. **JSON 로그 패턴** (edev 파일, dev, prod)

```json
{
  "timestamp": "2025-01-23 14:30:25.123",
  "level": "INFO",
  "thread": "http-nio-8080-exec-1",
  "logger": "com.skax.aiplatform.controller.AuthController",
  "traceId": "abc123def456",
  "spanId": "span789",
  "parentSpanId": "parent123",
  "userId": "testuser01",
  "requestUri": "/api/auth/login",
  "requestMethod": "POST",
  "clientIp": "192.168.1.100",
  "userAgent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64)",
  "message": "사용자 로그인 요청 처리 완료",
  "exception": ""
}
```

**JSON 필드 설명:**
- `timestamp`: 로그 발생 시간
- `level`: 로그 레벨 (ERROR, WARN, INFO, DEBUG, TRACE)
- `thread`: 스레드명
- `logger`: 로거 클래스명
- `traceId/spanId`: 분산 트레이싱 ID (추후 확장 가능)
- `userId`: 사용자 식별자 (MDC 컨텍스트)
- `requestUri/requestMethod`: HTTP 요청 정보
- `clientIp/userAgent`: 클라이언트 정보
- `message`: 실제 로그 메시지
- `exception`: 예외 정보 (발생시에만)

---

## 📊 환경별 로그 레벨 분석

### 로그 레벨 적용 매트릭스

| 로거/환경 | elocal | local | edev | dev | prod |
|-----------|--------|--------|------|-----|------|
| **Root** | WARN | WARN | WARN | INFO | WARN |
| **com.skax.aiplatform** | DEBUG | DEBUG | DEBUG | INFO | WARN |
| **org.springframework** | INFO | INFO | INFO | WARN | ERROR |
| **org.hibernate.SQL** | DEBUG | DEBUG | DEBUG | - | - |
| **org.hibernate.orm.jdbc.bind** | TRACE | TRACE | TRACE | - | - |
| **com.zaxxer.hikari** | WARN | WARN | - | - | - |
| **org.postgresql** | - | - | INFO | - | - |
| **com.tmax.tibero** | - | - | - | WARN | ERROR |

### 로그 레벨별 상세 분석

#### 🔍 **DEBUG 레벨** (개발 환경 전용)
- **대상 환경**: elocal, local, edev
- **주요 용도**: 
  - 개발자 디버깅
  - SQL 쿼리 확인
  - 메서드 실행 흐름 추적
- **예시 로그**:
  ```
  2025-01-23 14:30:25.123 DEBUG c.s.a.service.UserService: getUserById 호출 - userId: 12345
  2025-01-23 14:30:25.125 DEBUG org.hibernate.SQL: select user0_.id as id1_0_ from users user0_ where user0_.id=?
  2025-01-23 14:30:25.126 TRACE o.h.orm.jdbc.bind: binding parameter [1] as [BIGINT] - [12345]
  ```

#### ℹ️ **INFO 레벨** (일반 정보)
- **대상 환경**: 모든 환경
- **주요 용도**:
  - 애플리케이션 상태 정보
  - 비즈니스 로직 실행 결과
  - 외부 API 호출 성공/실패
- **예시 로그**:
  ```json
  {
    "timestamp": "2025-01-23 14:30:25.123",
    "level": "INFO",
    "logger": "com.skax.aiplatform.service.AuthService",
    "message": "SKTAI 인증 성공 - username: testuser01, duration: 1250ms"
  }
  ```

#### ⚠️ **WARN 레벨** (경고)
- **대상 환경**: 모든 환경
- **주요 용도**:
  - 비정상적이지만 처리 가능한 상황
  - 성능 이슈 경고
  - 외부 시스템 일시적 오류
- **예시 로그**:
  ```json
  {
    "timestamp": "2025-01-23 14:30:25.123",
    "level": "WARN",
    "logger": "com.skax.aiplatform.client.SktaiAuthService",
    "message": "SKTAI API 응답 지연 - duration: 5200ms, threshold: 3000ms"
  }
  ```

#### ❌ **ERROR 레벨** (오류)
- **대상 환경**: 모든 환경 (별도 파일 저장)
- **주요 용도**:
  - 시스템 오류
  - 예외 발생
  - 복구 불가능한 상황
- **예시 로그**:
  ```json
  {
    "timestamp": "2025-01-23 14:30:25.123",
    "level": "ERROR",
    "logger": "com.skax.aiplatform.service.UserService",
    "message": "사용자 생성 실패 - 데이터베이스 연결 오류",
    "exception": "java.sql.SQLException: Connection timeout\n\tat com.zaxxer.hikari..."
  }
  ```

---

## 🔧 특별 로그 카테고리

### 1. **SQL 로깅** (개발 환경 전용)

#### **설정 위치**
- `application.yml` (공통): `logging.level.[org.hibernate.SQL]: DEBUG`
- `logback-spring.xml` (elocal, local): SQL 관련 로거 설정

#### **출력 예시**
```
2025-01-23 14:30:25.123 DEBUG org.hibernate.SQL: 
    select 
        user0_.id as id1_15_0_,
        user0_.username as username2_15_0_,
        user0_.email as email3_15_0_,
        user0_.created_at as created_4_15_0_ 
    from 
        users user0_ 
    where 
        user0_.id=?

2025-01-23 14:30:25.124 TRACE org.hibernate.orm.jdbc.bind: 
    binding parameter [1] as [BIGINT] - [12345]
```

#### **SQL 로깅 제어**
```yaml
# 개발 환경 (elocal, local, edev)
logging:
  level:
    "[org.hibernate.SQL]": DEBUG                    # SQL 쿼리 출력
    "[org.hibernate.orm.jdbc.bind]": TRACE          # 파라미터 바인딩 출력
    "[org.hibernate.type.descriptor.sql]": TRACE    # 타입 정보 출력

# 운영 환경 (dev, prod)
logging:
  level:
    "[org.hibernate.SQL]": ERROR                    # SQL 로깅 비활성화
```

### 2. **성능 로그** (prod 환경 전용)

#### **필터링 조건**
```xml
<filter class="ch.qos.logback.core.filter.EvaluatorFilter">
    <evaluator>
        <expression>message.contains("REQUEST_END") || message.contains("API_CALL_END")</expression>
    </evaluator>
    <onMatch>ACCEPT</onMatch>
    <onMismatch>DENY</onMismatch>
</filter>
```

#### **성능 로그 예시**
```json
{
  "timestamp": "2025-01-23 14:30:25.123",
  "level": "INFO",
  "logger": "com.skax.aiplatform.filter.PerformanceFilter",
  "requestUri": "/api/users/12345",
  "requestMethod": "GET",
  "message": "REQUEST_END - duration: 1250ms, status: 200"
}
```

### 3. **감사 로그** (prod 환경 전용)

#### **필터링 조건**
```xml
<filter class="ch.qos.logback.core.filter.EvaluatorFilter">
    <evaluator>
        <expression>loggerName.contains("AUDIT") || message.contains("LOGIN") || message.contains("LOGOUT") || message.contains("ACCESS_DENIED")</expression>
    </evaluator>
    <onMatch>ACCEPT</onMatch>
    <onMismatch>DENY</onMismatch>
</filter>
```

#### **감사 로그 예시**
```json
{
  "timestamp": "2025-01-23 14:30:25.123",
  "level": "INFO",
  "logger": "AUDIT",
  "userId": "testuser01",
  "clientIp": "192.168.1.100",
  "userAgent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64)",
  "message": "LOGIN - 사용자 로그인 성공"
}
```

---

## 📈 로그 볼륨 및 보관 정책

### 환경별 로그 볼륨 예상치

| 환경 | 일일 로그 볼륨 | 주요 로그 유형 | 보관 기간 |
|------|----------------|----------------|-----------|
| **elocal** | N/A (콘솔만) | 개발 디버그 로그 | N/A |
| **local** | N/A (콘솔만) | 개발 디버그 로그 | N/A |
| **edev** | ~500MB | 외부 개발 테스트 로그 | 14일 |
| **dev** | ~1GB | 개발환경 통합 테스트 | 30일 |
| **prod** | ~5GB | 운영 트래픽 로그 | 30-90일 |

### 로그 파일 크기 제한

```yaml
# 공통 설정
MAX_FILE_SIZE: 100MB          # 개별 파일 최대 크기
MAX_HISTORY: 30               # 롤링 파일 보관 개수
TOTAL_SIZE_CAP: 3GB           # 전체 로그 파일 용량 제한
```

### 특수 보관 정책

#### **운영 환경 (prod) 차별화**
- **일반 로그**: 30일 보관, 3GB 제한
- **에러 로그**: 60일 보관, 2GB 제한 (문제 분석 기간 확보)
- **감사 로그**: 90일 보관, 5GB 제한 (보안 규정 준수)
- **성능 로그**: 30일 보관, 2GB 제한

---

## 🌐 외부 시스템 연동 로깅

### 외부 API 호출 로깅 패턴

AX Portal Backend는 다음 7개 주요 외부 시스템과 연동하며, 각각에 대한 로깅이 설정되어 있습니다:

#### 1. **SKTAI 플랫폼** (19개 서브모듈)
```java
// 로깅 예시
log.info("SKTAI 인증 요청 - username: {}", username);
log.info("SKTAI 모델 조회 성공 - modelId: {}, duration: {}ms", modelId, duration);
log.error("SKTAI API 호출 실패 - endpoint: {}, error: {}", endpoint, error.getMessage());
```

#### 2. **Lablup 연동**
```java
// 배치 처리 로깅
log.info("🔴 Lablup 배치 아티팩트 모델 스캔 요청");
log.info("🔴 Lablup 배치 아티팩트 모델 스캔 성공");
```

#### 3. **Datumo 연동**
```java
log.info("Datumo 평가 요청 - projectId: {}", projectId);
```

#### 4. **기타 외부 시스템** (iONE, Elastic, UDP, DeepSecurity)
```java
log.debug("외부 API 호출 - system: {}, endpoint: {}", system, endpoint);
```

### Feign Client 로깅 설정

```yaml
# application.yml (공통)
feign:
  client:
    config:
      default:
        logger-level: basic      # NONE, BASIC, HEADERS, FULL
      authClient:
        logger-level: full       # 인증 클라이언트는 상세 로깅

# 환경별 오버라이드
# dev 환경: basic 레벨 (성능 고려)
# prod 환경: none 레벨 (보안 및 성능)
```

### 외부 API 로깅 형식

```json
{
  "timestamp": "2025-01-23 14:30:25.123",
  "level": "INFO",
  "logger": "com.skax.aiplatform.client.sktai.auth.SktaiAuthService",
  "traceId": "trace-123",
  "message": "SKTAI 인증 API 호출",
  "apiEndpoint": "https://adxp.mobigen.com/v1.1/auth/login",
  "httpMethod": "POST",
  "duration": "1250ms",
  "responseStatus": "200",
  "success": true
}
```

---

## 🚀 성능테스트 관련 로깅 최적화

### 개발환경 성능테스트 설정 (dev)

#### **모니터링 엔드포인트 로깅**
```yaml
# application-dev.yml
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus,caches,httptrace
  metrics:
    distribution:
      percentiles-histogram:
        "[http.server.requests]": true
      percentiles:
        "[http.server.requests]": 0.5,0.9,0.95,0.99
```

#### **성능 관련 로그 레벨**
```yaml
logging:
  level:
    "[com.skax.aiplatform]": INFO           # 성능테스트용 INFO 레벨
    "[com.zaxxer.hikari]": WARN             # 커넥션 풀 모니터링
    "[org.springframework.transaction]": WARN  # 트랜잭션 성능 모니터링
```

### 운영환경 성능테스트 설정 (prod)

#### **보안을 고려한 제한적 모니터링**
```yaml
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus  # httptrace, caches 제외
  endpoint:
    health:
      show-details: when-authorized  # 인증된 사용자에게만 상세 정보
```

#### **성능 최적화된 로그 레벨**
```yaml
logging:
  level:
    root: ERROR                           # 시스템 로그 최소화
    "[com.skax.aiplatform]": INFO         # 필요한 비즈니스 로그만
    "[com.zaxxer.hikari]": WARN           # 커넥션 풀 경고만
    "[org.hibernate.SQL]": ERROR         # SQL 로깅 완전 비활성화
```

---

## 📋 환경별 적용 가능한 로깅 설정 매트릭스

### 로깅 기능별 환경 적용 현황

| 기능 | elocal | local | edev | dev | prod | 비고 |
|------|--------|--------|------|-----|------|------|
| **콘솔 출력** | ✅ | ✅ | ✅ | ❌ | ❌ | 개발환경만 |
| **파일 출력** | ❌ | ❌ | ✅ | ✅ | ✅ | 테스트/운영환경 |
| **JSON 형식** | ❌ | ❌ | ✅ | ✅ | ✅ | 구조화된 로그 |
| **SQL 로깅** | ✅ | ✅ | ✅ | ❌ | ❌ | 개발환경만 |
| **에러 전용 파일** | ❌ | ❌ | ❌ | ✅ | ✅ | 개발/운영환경 |
| **성능 로그** | ❌ | ❌ | ❌ | ❌ | ✅ | prod만 |
| **감사 로그** | ❌ | ❌ | ❌ | ❌ | ✅ | 운영환경만 |
| **로그 압축** | ❌ | ❌ | ❌ | ❌ | ❌ | 미구현 |
| **로그 전송** | ❌ | ❌ | ❌ | ❌ | ❌ | 미구현 |

### 로거별 레벨 설정 매트릭스

| 로거 카테고리 | elocal | local | edev | dev | prod |
|---------------|--------|--------|------|-----|------|
| **Application** | DEBUG | DEBUG | DEBUG | INFO | WARN |
| **SQL** | DEBUG | DEBUG | DEBUG | OFF | OFF |
| **Security** | INFO | INFO | INFO | WARN | ERROR |
| **External API** | DEBUG | DEBUG | DEBUG | INFO | WARN |
| **Batch** | DEBUG | DEBUG | DEBUG | INFO | WARN |
| **Performance** | - | - | - | - | INFO |
| **Audit** | - | - | - | - | INFO |

---

## 🔍 로그 분석 및 모니터링 가이드

### 1. **개발 단계별 로그 활용법**

#### **로컬 개발 (elocal, local)**
```bash
# 실시간 로그 모니터링 (콘솔 출력)
# IDE에서 직접 확인 또는 터미널 실행
java -jar axportal-backend.jar --spring.profiles.active=elocal
```

#### **통합 테스트 (edev)**
```bash
# 파일 로그 실시간 모니터링
tail -f logs/axportal-backend.log

# JSON 로그 파싱
cat logs/axportal-backend.log | jq '.message'
```

#### **개발 환경 (dev)**
```bash
# 에러 로그 모니터링
tail -f logs/axportal-backend-error.json

# 특정 시간대 로그 필터링
cat logs/axportal-backend.json | jq 'select(.timestamp | startswith("2025-01-23 14:"))'
```

#### **운영 환경 (prod)**
```bash
# 감사 로그 확인
tail -f logs/axportal-backend-audit.json

# 성능 이슈 분석
cat logs/axportal-backend-performance.json | jq 'select(.message | contains("duration")) | select(.duration > "3000ms")'
```

### 2. **로그 검색 및 분석 쿼리**

#### **사용자별 로그 추적**
```bash
# 특정 사용자의 모든 활동 로그
cat logs/axportal-backend.json | jq 'select(.userId == "testuser01")'
```

#### **API 응답 시간 분석**
```bash
# 3초 이상 소요된 API 호출
cat logs/axportal-backend-performance.json | jq 'select(.message | contains("duration")) | select(.message | contains("ms")) | select(.message | split("ms")[0] | split(": ")[1] | tonumber > 3000)'
```

#### **에러 패턴 분석**
```bash
# 특정 시간대 에러 빈도
cat logs/axportal-backend-error.json | jq -r '.timestamp' | grep "2025-01-23 14:" | wc -l
```

### 3. **성능테스트 모니터링**

#### **실시간 성능 지표 확인**
```bash
# HTTP 요청 통계
curl http://localhost:8080/api/actuator/metrics/http.server.requests

# 데이터베이스 커넥션 풀 상태
curl http://localhost:8080/api/actuator/metrics/hikaricp.connections
```

#### **로그 기반 성능 분석**
```bash
# 평균 응답시간 계산
cat logs/axportal-backend-performance.json | jq -r '.message | select(contains("duration")) | split("duration: ")[1] | split("ms")[0]' | awk '{sum+=$1; count++} END {print "Average:", sum/count, "ms"}'
```

---

## ⚙️ 설정 최적화 권장사항

### 1. **개발 효율성 향상**

#### **로컬 개발 환경 최적화**
```xml
<!-- logback-spring.xml 개선 제안 -->
<springProfile name="elocal,local">
    <!-- 컬러풀 콘솔 출력 -->
    <appender name="COLORED_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%cyan(%d{yyyy-MM-dd HH:mm:ss.SSS}) %highlight(%5p) %magenta(%-40.40logger{39}): %msg%n%ex</pattern>
        </encoder>
    </appender>
</springProfile>
```

#### **개발자 친화적 로그 레벨**
```yaml
# application-elocal.yml 개선 제안
logging:
  level:
    "[com.skax.aiplatform.controller]": DEBUG      # API 호출 추적
    "[com.skax.aiplatform.service]": DEBUG         # 비즈니스 로직 추적
    "[com.skax.aiplatform.client]": INFO           # 외부 API 호출만
```

### 2. **운영 안정성 강화**

#### **알림 연동 설정**
```xml
<!-- 운영환경 SMTP 알림 Appender 추가 제안 -->
<appender name="SMTP" class="ch.qos.logback.classic.net.SMTPAppender">
    <smtpHost>smtp.company.com</smtpHost>
    <to>dev-team@company.com</to>
    <from>axportal-prod@company.com</from>
    <subject>AX Portal 운영 오류 발생</subject>
    <layout class="ch.qos.logback.classic.html.HTMLLayout"/>
    <filter class="ch.qos.logback.classic.filter.LevelFilter">
        <level>ERROR</level>
        <onMatch>ACCEPT</onMatch>
        <onMismatch>DENY</onMismatch>
    </filter>
</appender>
```

#### **로그 압축 및 아카이빙**
```xml
<!-- 압축 정책 개선 제안 -->
<rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
    <fileNamePattern>${LOG_PATH}/${LOG_FILE}-%d{yyyy-MM-dd}-%i.json.gz</fileNamePattern>
    <maxFileSize>${MAX_FILE_SIZE}</maxFileSize>
    <maxHistory>${MAX_HISTORY}</maxHistory>
    <totalSizeCap>${TOTAL_SIZE_CAP}</totalSizeCap>
    <cleanHistoryOnStart>true</cleanHistoryOnStart>
</rollingPolicy>
```

### 3. **성능 최적화**

#### **비동기 로깅 적용**
```xml
<!-- AsyncAppender 추가 제안 -->
<appender name="ASYNC_FILE" class="ch.qos.logback.classic.AsyncAppender">
    <appender-ref ref="JSON_FILE"/>
    <queueSize>512</queueSize>
    <discardingThreshold>20</discardingThreshold>
    <maxFlushTime>1000</maxFlushTime>
    <neverBlock>true</neverBlock>
</appender>
```

#### **MDC를 활용한 컨텍스트 로깅**
```java
// 구현 제안: 요청별 트레이싱
@Component
public class LoggingFilter implements Filter {
    @Override
    public void doFilter(ServletRequest request, ServletResponse response, FilterChain chain) {
        try {
            MDC.put("traceId", UUID.randomUUID().toString());
            MDC.put("requestUri", ((HttpServletRequest) request).getRequestURI());
            chain.doFilter(request, response);
        } finally {
            MDC.clear();
        }
    }
}
```

---

## 📊 결론 및 요약

### 현재 로깅 시스템 평가

#### **✅ 장점**
1. **환경별 차별화**: 5개 환경에 최적화된 로깅 설정
2. **JSON 형식 지원**: 구조화된 로그로 분석 용이성
3. **파일 분리**: 에러, 성능, 감사 로그 별도 관리
4. **롤링 정책**: 디스크 공간 효율적 관리
5. **개발자 친화적**: 콘솔 출력으로 실시간 디버깅 지원

#### **⚠️ 개선 필요 사항**
1. **비동기 로깅 미적용**: 성능 영향 최소화 필요
2. **로그 압축 미설정**: 저장 공간 효율성 개선
3. **중앙집중식 로깅 부재**: ELK Stack 등 연동 고려
4. **알림 시스템 부재**: 실시간 오류 알림 체계 구축
5. **로그 레벨 동적 변경**: 운영 중 로그 레벨 조정 기능

### 환경별 로깅 전략 요약

| 환경 | 주요 특징 | 적용 시나리오 |
|------|-----------|---------------|
| **elocal** (기본) | 콘솔 출력, DEBUG 레벨, PostgreSQL 연동 | 외부 로컬 개발, 실시간 디버깅 |
| **local** | 콘솔 출력, SQL 로깅, Tibero 연동 | 로컬 환경 테스트 |
| **edev** | 파일+콘솔, JSON 형식, PostgreSQL 연동 | 외부 개발 환경 통합 테스트 |
| **dev** | 파일 전용, 에러 분리, Tibero 연동 | 개발 환경 성능 테스트 |
| **prod** | 다중 파일, 감사 로그, Tibero 연동, 보안 강화 | 운영 환경 모니터링 |

### 성능테스트 관련 로깅 준비 상태

#### **✅ 준비 완료**
- 개발/운영환경 성능 모니터링 엔드포인트 활성화
- JSON 형식 구조화된 로그 수집 가능
- 성능 로그 별도 파일 저장 (prod)
- HTTP 요청 응답시간 백분위수 수집

#### **🔧 추가 권장사항**
- 비동기 로깅 적용으로 성능 오버헤드 최소화
- 로그 샘플링 정책 도입 (고부하시 로그량 제어)
- 실시간 성능 지표 대시보드 연동
- 성능 임계값 기반 자동 알림 설정

---

**보고서 작성자**: GitHub Copilot  
**최종 업데이트**: 2025년 11월 14일  
**문서 버전**: 1.0

---

*이 보고서는 AX Portal Backend 프로젝트의 로깅 시스템을 종합 분석한 결과물입니다. 성능테스트 및 운영 모니터링에 활용하시기 바랍니다.*